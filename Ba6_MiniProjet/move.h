#ifndef MOVE_H
#define MOVE_H

// Fonction to control the robot with the sound
void move(float rotation);
void move_stop(void);

// Set a new speed
void set_speed(float new_speed);

#endif /* MOVE_H */
